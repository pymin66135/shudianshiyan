# Portable Package Notes

This version is part of the Stage 3.5 portable matrix repair.

Use `scripts/create_project.tcl` to recreate the Vivado project from local files. Do not use quarantined legacy `.xpr` files.

Required local resources in this package:

- `rtl/`
- `tb/`
- `constraints/`
- `ip/`
- `coe/test_rom.coe`
- `scripts/`
- `README.md`